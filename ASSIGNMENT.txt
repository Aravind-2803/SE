ASSIGNMENT - I
USING IF-ELSE STATEMENT
Check whether two numbers are equal

java
import java.util.Scanner;
class EqualNumbers {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
        if (a == b)
            System.out.println("Numbers are Equal");
        else
            System.out.println("Numbers are Not Equal");
    }
}
Leap year or not

java
import java.util.Scanner;
class LeapYear {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int year = sc.nextInt();
        if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0)
            System.out.println("Leap Year");
        else
            System.out.println("Not a Leap Year");
    }
}
Odd or Even

java
import java.util.Scanner;
class OddEven {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        if (n % 2 == 0)
            System.out.println("Even");
        else
            System.out.println("Odd");
    }
}
Positive or Negative

java
import java.util.Scanner;
class PositiveNegative {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        if (n >= 0)
            System.out.println("Positive");
        else
            System.out.println("Negative");
    }
}
Biggest among three numbers

java
import java.util.Scanner;
class BiggestOfThree {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt(), b = sc.nextInt(), c = sc.nextInt();
        if (a > b && a > c)
            System.out.println(a + " is largest");
        else if (b > c)
            System.out.println(b + " is largest");
        else
            System.out.println(c + " is largest");
    }
}
Sort three numbers

java
import java.util.Scanner;
class SortThree {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt(), b = sc.nextInt(), c = sc.nextInt();
        if (a > b) { int t = a; a = b; b = t; }
        if (a > c) { int t = a; a = c; c = t; }
        if (b > c) { int t = b; b = c; c = t; }
        System.out.println(a + " " + b + " " + c);
    }
}
Valid Triangle

java
import java.util.Scanner;
class TriangleValid {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt(), b = sc.nextInt(), c = sc.nextInt();
        if (a + b + c == 180)
            System.out.println("Valid Triangle");
        else
            System.out.println("Invalid Triangle");
    }
}
Electricity Bill

java
import java.util.Scanner;
// Note: The class definition is missing in the snippet. Assuming public static void main...
// Scanner sc = new Scanner(System.in);
// int units = sc.nextInt();
// double bill;
// if (units < 100) bill = units * 2.5;
// else if (units <= 250) bill = units * 4.0;
// else bill = units * 5.0;
// System.out.println("Bill Amount = Rs." + bill);
Student Grade

java
import java.util.Scanner;
class Grade {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int mark = sc.nextInt(); // Typo in file: sc.nextlnt()
        if (mark >= 90) System.out.println("Grade S");
        else if (mark >= 80) System.out.println("Grade A");
        else if (mark >= 70) System.out.println("Grade B");
        else if (mark >= 60) System.out.println("Grade C");
        else if (mark >= 50) System.out.println("Grade D");
        else System.out.println("Grade U");
    }
}
Maximum and Minimum (Only declaration provided, code incomplete)

Gross Pay & Net Pay

java
import java.util.Scanner;
class Salary {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        double basic = sc.nextDouble();
        double hra = (basic > 15000) ? basic * 0.10 : basic * 0.05;
        double dp = basic * 0.10;
        double da = (basic + dp) * 0.18;
        double cca = 500, ma = 200;
        double gpf = (basic + dp) * 0.20;
        double fbf = (basic + dp) * 0.10;
        double it = 700;
        double gross = basic + dp + da + hra + cca + ma;
        double net = gross - (gpf + fbf + it);
        System.out.println("Gross Pay = " + gross);
        System.out.println("Net Pay = " + net);
    }
}
Roots of Quadratic Equation

java
import java.util.Scanner;
class Quadratic {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        double a = sc.nextDouble(), b = sc.nextDouble(), c = sc.nextDouble();
        double d = b*b - 4*a*c;
        if (d >= 0) {
            double r1 = (-b + Math.sqrt(d)) / (2*a);
            double r2 = (-b - Math.sqrt(d)) / (2*a);
            System.out.println("Roots: " + r1 + ", " + r2);
        } else {
            System.out.println("No Real Roots");
        }
    }
}
USING FOR LOOP
Sum of digits (1234 -> 10)

java
import java.util.Scanner;
class SumOfDigits {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt(), sum = 0;
        for (; n > 0; n /= 10) sum += n % 10;
        System.out.println("Sum = " + sum);
    }
}
Factorial of a number

java
import java.util.Scanner;
class Factorial {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        long fact = 1;
        for (int i = 1; i <= n; i++) fact *= i;
        System.out.println("Factorial = " + fact);
    }
}
Fibonacci term for a given number

java
import java.util.Scanner;
class FibonacciTerm {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int a = 0, b = 1, c = 0;
        for (int i = 2; i <= n; i++) {
            c = a + b;
            a = b;
            b = c;
        }
        System.out.println("Term = " + b);
    }
}
Fibonacci series up to n terms

java
import java.util.Scanner;
class FibonacciSeries {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int a = 0, b = 1;
        System.out.print(a + " " + b);
        for (int i = 3; i <= n; i++) {
            int c = a + b;
            System.out.print(" " + c);
            a = b;
            b = c;
        }
    }
}
Right angle triangle (stars)

java
class StarTriangle {
    public static void main(String[] args) {
        for (int i = 1; i <= 3; i++) {
            for (int j = 1; j <= i; j++)
                System.out.print("* ");
            System.out.println();
        }
    }
}
Number pattern (i)

java
class Pattern1 {
    public static void main(String[] args) {
        int n = 3;
        for (int i = 1; i <= n; i++) {
            for (int s = n - i; s > 0; s--) System.out.print(" ");
            for (int j = 1; j <= i; j++) System.out.print(i + " ");
            System.out.println();
        }
    }
}
(ii)

java
class Pattern2 {
    public static void main(String[] args) {
        for (int i = 1; i <= 3; i++) {
            for (int j = 1; j <= i; j++) System.out.print(j + " ");
            System.out.println();
        }
    }
}
Pattern (stars with spaces)

java
class Pattern7 {
    public static void main(String[] args) {
        int n = 3;
        for (int i = 1; i <= n; i++) {
            for (int s = n - i; s > 0; s--) System.out.print(" ");
            for (int j = 1; j <= i; j++) System.out.print(" * ");
            System.out.println();
        }
    }
}JAVA ASSIGNMENT - II
1. Position of the biggest number in an array
java
import java.util.Scanner;
class BiggestPosition {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n, max, pos = 0;
        System.out.print("Enter number of elements: ");
        n = sc.nextInt();
        int a[] = new int[n];
        for (int i = 0; i < n; i++) {
            a[i] = sc.nextInt();
        }
        max = a[0];
        for (int i = 1; i < n; i++) {
            if (a[i] > max) {
                max = a[i];
                pos = i;
            }
        }
    }
}
2. Reverse a given number using array
java
import java.util.Scanner;
class reverse {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int num, i = 0;
        System.out.print("Enter number: ");
        num = sc.nextInt();
        int a[] = new int[10];
        while (num > 0) {
            a[i++] = num % 10;
            num = num / 10;
        }
        System.out.print("Reversed number: ");
        for (int j = 0; j < i; j++) {
            System.out.print(a[j]);
        }
    }
}
3. Total and average of n numbers
Note: The file has a mismatch. The title says "Total and average" but the code calculates "Sum of squares". The correct "totalaverage" class appears on page 4 but is incomplete.

Sum of squares (provided under #3):

java
import java.util.Scanner;
class SumOfSquares {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n, sum = 0;
        System.out.print("Enter n: ");
        n = sc.nextInt();
        int a[] = new int[n];
        for (int i = 0; i < n; i++) {
            a[i] = sc.nextInt();
            sum += a[i] * a[i];
        }
        System.out.println("Sum of squares = " + sum);
    }
}
4. Matrix Multiplication
java
import java.util.Scanner;
class matrixmultiplication {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int r1 = sc.nextInt(), c1 = sc.nextInt();
        int r2 = sc.nextInt(), c2 = sc.nextInt();
        int a[][] = new int[r1][c1];
        int b[][] = new int[r2][c2];
        int mul[][] = new int[r1][c2];
        for (int i = 0; i < r1; i++)
            for (int j = 0; j < c1; j++)
                a[i][j] = sc.nextInt();
        for (int i = 0; i < r2; i++)
            for (int j = 0; j < c2; j++)
                b[i][j] = sc.nextInt();
        // Note: Actual multiplication logic is missing in the snippet
    }
}
9. Transpose of a matrix
java
import java.util.Scanner;
class TransposeMatrix {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int r = sc.nextInt();
        int c = sc.nextInt();
        int a[][] = new int[r][c];
        for (int i = 0; i < r; i++)
            for (int j = 0; j < c; j++)
                a[i][j] = sc.nextInt();
        for (int j = 0; j < c; j++) {
            for (int i = 0; i < r; i++)
                System.out.print(a[i][j] + " ");
            System.out.println();
        }
    }
}
10. Matrix Rotation (90°, 180°, 270°)
java
import java.util.Scanner;
class MatrixRotation {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n;
        System.out.print("Enter order of square matrix: ");
        n = sc.nextInt();
        int a[][] = new int[n][n];
        System.out.println("Enter matrix elements:");
        for (int i = 0; i < n; i++)
            for (int j = 0; j < n; j++)
                a[i][j] = sc.nextInt();
        System.out.println("\nOriginal Matrix:");
        printMatrix(a, n);
        System.out.println("\n90 Degree Rotation:");
        rotate90(a, n);
        System.out.println("\n180 Degree Rotation:");
        rotate180(a, n);
        System.out.println("\n270 Degree Rotation:");
        rotate270(a, n);
    }
    static void printMatrix(int a[][], int n) {
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++)
                System.out.print(a[i][j] + " ");
            System.out.println();
        }
    }
    static void rotate90(int a[][], int n) {
        for (int j = 0; j < n; j++) {
            for (int i = n - 1; i >= 0; i--)
                System.out.print(a[i][j] + " ");
            System.out.println();
        }
    }
    static void rotate180(int a[][], int n) {
        for (int i = n - 1; i >= 0; i--) {
            for (int j = n - 1; j >= 0; j--)
                System.out.print(a[i][j] + " ");
            System.out.println();
        }
    }
    static void rotate270(int a[][], int n) {
        for (int j = n - 1; j >= 0; j--) {
            for (int i = 0; i < n; i++)
                System.out.print(a[i][j] + " ");
            System.out.println();
        }
    }
}
11. Sort numbers in ascending order (using Arrays.sort)
java
import java.util.Scanner;
import java.util.Arrays;
class ascendingsort {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int a[] = new int[n];
        for (int i = 0; i < n; i++)
            a[i] = sc.nextInt();
        Arrays.sort(a);
        for (int i : a)
            System.out.print(i + " ");
    }
}
12. Bubble sort
java
import java.util.Scanner;
class bubblesort {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int a[] = new int[n];
        for (int i = 0; i < n; i++)
            a[i] = sc.nextInt();
        for (int i = 0; i < n - 1; i++) {
            for (int j = 0; j < n - i - 1; j++) {
                if (a[j] > a[j + 1]) {
                    int temp = a[j];
                    a[j] = a[j + 1];
                    a[j + 1] = temp;
                }
            }
        }
        for (int i : a)
            System.out.print(i + " ");
    }
}
JAVA ASSIGNMENT - III
1. Convert lowercase to uppercase
java
import java.util.Scanner;
class LowerToUpper {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        System.out.println(str.toUpperCase());
    }
}
2. Count number of vowels in a string
java
import java.util.Scanner;
class CountVowels {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine().toLowerCase();
        int count = 0;
        for (char c : str.toCharArray()) {
            if ("aeiou".indexOf(c) != -1) count++;
        }
        System.out.println("Vowels: " + count);
    }
}
3. Check palindrome
java
import java.util.Scanner;
class Palindrome {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        String rev = new StringBuilder(str).reverse().toString();
        if (str.equals(rev)) {
            System.out.println("Palindrome");
        } else {
            System.out.println("Not Palindrome");
        }
    }
}
4. Change case of a string (toggle case)
java
import java.util.Scanner;
class ChangeCase {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        StringBuilder sb = new StringBuilder();
        for (char c : str.toCharArray()) {
            if (Character.isUpperCase(c))
                sb.append(Character.toLowerCase(c));
            else
                sb.append(Character.toUpperCase(c));
        }
        System.out.println(sb);
    }
}
5. Count characters, words, and lines
java
import java.util.Scanner;
class CountAll {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        int chars = str.length();
        int words = str.trim().split("\\s+").length;
        System.out.println("Characters: " + chars);
        System.out.println("Words: " + words);
        System.out.println("Lines: 1");
    }
}
6. Sort strings alphabetically (predefined array)
java
import java.util.Arrays;
class SortStrings {
    public static void main(String[] args) {
        String[] arr = {"banana", "apple", "cherry"};
        Arrays.sort(arr);
        for (String s : arr)
            System.out.println(s);
    }
}
7. Sort strings using array (user input)
java
import java.util.Arrays;
import java.util.Scanner;
class SortStringArray {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        sc.nextLine();
        String[] arr = new String[n];
        for (int i = 0; i < n; i++)
            arr[i] = sc.nextLine();
        Arrays.sort(arr);
        for (String s : arr)
            System.out.println(s);
    }
}
8. Check anagram
java
import java.util.Arrays;
import java.util.Scanner;
class Anagram {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String s1 = sc.nextLine();
        String s2 = sc.nextLine();
        char[] a = s1.toCharArray();
        char[] b = s2.toCharArray();
        Arrays.sort(a);
        Arrays.sort(b);
        System.out.println(Arrays.equals(a, b) ? "Anagram" : "Not Anagram");
    }
}
9. Replace character at specific index
java
import java.util.Scanner;
class ReplaceChar {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        int index = sc.nextInt();
        char ch = sc.next().charAt(0);
        String result = str.substring(0, index) + ch + str.substring(index + 1);
        System.out.println(result);
    }
}
10. Remove leading zeros
java
import java.util.Scanner;
class RemoveLeadingZeros {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        // Correction: The original had "O+" (letter O) instead of "0+" (zero)
        System.out.println(str.replaceFirst("^0+(?!$)", ""));
    }
}
11. Compare two strings
(Code incomplete in file - only class declaration and first string input)

java
import java.util.Scanner;
class CompareStrings {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String s1 = sc.nextLine();
        String s2 = sc.nextLine();
        if (s1.equals(s2))
            System.out.println("Equal");
        else
            System.out.println("Not Equal");
    }
}
12. Sum of two large numbers (using BigInteger)
java
import java.math.BigInteger;
import java.util.Scanner;
class LargeSum {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        BigInteger a = new BigInteger(sc.nextLine());
        BigInteger b = new BigInteger(sc.nextLine());
        System.out.println(a.add(b));
    }
}
13. Extract substrings (positions 0,1,2)
java
import java.util.Scanner;
class ExtractSubstring {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        System.out.println(str.substring(0));
        System.out.println(str.substring(1));
        System.out.println(str.substring(2));
    }
}
14. Add binary strings
java
class AddBinary {
    public static void main(String[] args) {
        String a = "1010";
        String b = "1101";
        int sum = Integer.parseInt(a, 2) + Integer.parseInt(b, 2);
        System.out.println(Integer.toBinaryString(sum));
    }
}
15. Validate IP address
(Code incomplete in file - regex pattern provided)

java
import java.util.Scanner;
class ValidateIP {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String ip = sc.nextLine();
        String regex = "^(25[0-5]|2[0-4]\\d|[01]?\\d\\d?)\\.{3}(25[0-5]|2[0-4]\\d|[01]?\\d\\d?)$";
        System.out.println(ip.matches(regex) ? "Valid IP" : "Invalid IP");
    }
}
16. Print all permutations of a string
java
class Permutations {
    static void permute(String str, String ans) {
        if (str.length() == 0) {
            System.out.println(ans);
            return;
        }
        for (int i = 0; i < str.length(); i++) {
            permute(str.substring(0, i) + str.substring(i + 1), ans + str.charAt(i));
        }
    }
    public static void main(String[] args) {
        String str = "abc";
        permute(str, "");
    }
}
17. Generate strong password
java
import java.util.Random;
class StrongPassword {
    public static void main(String[] args) {
        String upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
        String lower = "abcdefghijklmnopqrstuvwxyz";
        String numbers = "0123456789";
        String symbols = "!@#$%^&*";
        String all = upper + lower + numbers + symbols;
        Random r = new Random();
        StringBuilder password = new StringBuilder();
        for (int i = 0; i < 12; i++) {
            password.append(all.charAt(r.nextInt(all.length())));
        }
        System.out.println("Strong Password: " + password);
    }
}
JAVA ASSIGNMENT - IV
1. Abstract Class: Animal
java
abstract class Animal {
    abstract void sound();
}

class Lion extends Animal {
    public void sound() {
        System.out.println("Lion roars");
    }
}

class Tiger extends Animal {
    public void sound() {
        System.out.println("Tiger growls");
    }
}

public class MainAnimal {
    public static void main(String[] args) {
        Animal a1 = new Lion();
        Animal a2 = new Tiger();
        a1.sound();
        a2.sound();
    }
}
2. Abstract Class: Shape (Area & Perimeter)
java
abstract class Shape {
    abstract double calculateArea();
    abstract double calculatePerimeter();
}

class Circle extends Shape {
    double r = 5;
    double calculateArea() {
        return Math.PI * r * r;
    }
    double calculatePerimeter() {
        return 2 * Math.PI * r;
    }
}

class Triangle extends Shape {
    double a = 3, b = 4, c = 5;
    double calculateArea() {
        return 0.5 * a * b;
    }
    double calculatePerimeter() {
        return a + b + c;
    }
}

public class MainShape {
    public static void main(String[] args) {
        Shape s1 = new Circle();
        Shape s2 = new Triangle();
        System.out.println("Circle Area: " + s1.calculateArea());
        System.out.println("Triangle Perimeter: " + s2.calculatePerimeter());
    }
}
3. Abstract Class: BankAccount
java
abstract class BankAccount {
    double balance = 1000;
    abstract void deposit(double amount);
    abstract void withdraw(double amount);
}

class SavingsAccount extends BankAccount {
    void deposit(double amount) {
        balance += amount;
        System.out.println("Savings Balance: " + balance);
    }
    void withdraw(double amount) {
        balance -= amount;
        System.out.println("Savings Balance: " + balance);
    }
}

class CurrentAccount extends BankAccount {
    void deposit(double amount) {
        balance += amount;
        System.out.println("Current Balance: " + balance);
    }
    void withdraw(double amount) {
        balance -= amount;
        System.out.println("Current Balance: " + balance);
    }
}

public class MainBank {
    public static void main(String[] args) {
        BankAccount acc = new SavingsAccount();
        acc.deposit(500);
        acc.withdraw(200);
    }
}
4. Abstract Class: Employee
java
abstract class Employee {
    abstract double calculateSalary();
    abstract void displayInfo();
}

class Manager extends Employee {
    double salary = 50000;
    double calculateSalary() {
        return salary;
    }
    void displayInfo() {
        System.out.println("Manager Salary: " + salary);
    }
}

class Programmer extends Employee {
    double salary = 40000;
    double calculateSalary() {
        return salary;
    }
    void displayInfo() {
        System.out.println("Programmer Salary: " + salary);
    }
}

public class MainEmployee {
    public static void main(String[] args) {
        Employee e1 = new Manager();
        Employee e2 = new Programmer();
        e1.displayInfo();
        e2.displayInfo();
    }
}
5. Abstract Class: Shape3D (Sphere & Cube)
java
abstract class Shape3D {
    abstract double calculateVolume();
    abstract double calculateSurfaceArea();
}

class Sphere extends Shape3D {
    double r = 3;
    double calculateVolume() {
        return (4.0 / 3) * Math.PI * r * r * r;
    }
    double calculateSurfaceArea() {
        return 4 * Math.PI * r * r;
    }
}

class Cube extends Shape3D {
    double side = 4;
    double calculateVolume() {
        return side * side * side;
    }
    double calculateSurfaceArea() {
        return 6 * side * side;
    }
}

public class Main3D {
    public static void main(String[] args) {
        Shape3D s = new Sphere();
        System.out.println("Sphere Volume: " + s.calculateVolume());
    }
}
6. Abstract Class: Shape2D (Draw & Resize)
java
abstract class Shape2D {
    abstract void draw();
    abstract void resize();
}

class Rectangle extends Shape2D {
    void draw() {
        System.out.println("Drawing Rectangle");
    }
    void resize() {
        System.out.println("Resizing Rectangle");
    }
}

class Circle2D extends Shape2D {
    void draw() {
        System.out.println("Drawing Circle");
    }
    void resize() {
        System.out.println("Resizing Circle");
    }
}

public class Main2D {
    public static void main(String[] args) {
        Shape2D s = new Rectangle();
        s.draw();
        s.resize();
    }
}
7. Abstract Class: GeometricShape (Triangle & Square)
java
abstract class GeometricShape {
    abstract double area();
    abstract double perimeter();
}

class Triangle2 extends GeometricShape {
    double a = 3, b = 4, c = 5;
    double area() {
        return 0.5 * a * b;
    }
    double perimeter() {
        return a + b + c;
    }
}

class Square extends GeometricShape {
    double side = 4;
    double area() {
        return side * side;
    }
    double perimeter() {
        return 4 * side;
    }
}

public class MainGeo {
    public static void main(String[] args) {
        GeometricShape g = new Square();
        System.out.println("Square Area: " + g.area());
        System.out.println("Square Perimeter: " + g.perimeter());
    }
}
Program 1: Current Date/Time, Hour, Minute, Week of Year
java
package Mypackage;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.time.temporal.WeekFields;
import java.util.Locale;

public class date1 {
    public static void main(String[] args) {
        LocalDateTime now = LocalDateTime.now();
        System.out.println("Current Date/Time: " + now);
        System.out.println("Hour: " + now.getHour() + " Minute: " + now.getMinute());
        int weekOfYear = now.get(WeekFields.of(Locale.getDefault()).weekOfYear());
        System.out.println("Week of Year: " + weekOfYear);
    }
}
Program 2: AM/PM, 24-Hour Format, Seconds Only
java
package Mypackage;

import java.time.LocalTime;
import java.time.format.DateTimeFormatter;

public class date2 {
    public static void main(String[] args) {
        LocalTime time = LocalTime.now();
        System.out.println("AM/PM: " + time.format(DateTimeFormatter.ofPattern("hh:mm a")));
        String input = "02:30 PM";
        LocalTime parsed = LocalTime.parse(input, DateTimeFormatter.ofPattern("hh:mm a"));
        System.out.println("24-Hour Format: " + parsed.format(DateTimeFormatter.ofPattern("HH:mm")));
        System.out.println("Seconds only: " + time.format(DateTimeFormatter.ofPattern("ss")));
    }
}
Program 3: Month Names and All Days of Week
java
package Mypackage;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.time.DayOfWeek;
import java.util.Locale;

public class date3 {
    public static void main(String[] args) {
        LocalDateTime now = LocalDateTime.now();
        System.out.println("Month (MMM): " + now.format(DateTimeFormatter.ofPattern("MMM")));
        System.out.println("Month (MMMM): " + now.format(DateTimeFormatter.ofPattern("MMMM")));
        for (DayOfWeek day : DayOfWeek.values()) {
            System.out.print(day.getDisplayName(java.time.format.TextStyle.FULL, Locale.getDefault()) + " ");
        }
    }
}
Program 4: French Format and Tokyo Time
java
package Mypackage;

import java.time.ZonedDateTime;
import java.time.ZoneId;
import java.time.format.DateTimeFormatter;
import java.util.Locale;

public class date4 {
    public static void main(String[] args) {
        ZonedDateTime now = ZonedDateTime.now();
        DateTimeFormatter frenchFormatter = DateTimeFormatter.ofPattern("EEEE dd MMMM yyyy HH:mm", Locale.FRANCE);
        System.out.println("French Format: " + now.format(frenchFormatter));
        ZonedDateTime tokyoTime = ZonedDateTime.now(ZoneId.of("Asia/Tokyo"));
        System.out.println("Tokyo Time: " + tokyoTime.format(DateTimeFormatter.ofPattern("HH:mm")));
    }
}
Program 5: Future Date and Rolled Hour
java
package Mypackage;

import java.time.LocalDateTime;

public class date5 {
    public static void main(String[] args) {
        LocalDateTime now = LocalDateTime.now();
        LocalDateTime future = now.plusHours(2).plusDays(10);
        System.out.println("Future Date: " + future);
        int rolledHour = (now.getHour() + 5) % 24;
        System.out.println("Rolled Hour (current + 5): " + rolledHour);
    }
}
Program 6: Multiple Date Formats
java
package Mypackage;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class date6 {
    public static void main(String[] args) {
        LocalDateTime now = LocalDateTime.now();
        String[] formats = {"dd/MM/yyyy", "yyyy-MM-dd", "E, MMM dd yyyy", "MMMM dd"};
        for (String f : formats) {
            System.out.println(f + " -> " + now.format(DateTimeFormatter.ofPattern(f)));
        }
    }
}
Program 7: Current Seconds
java
package Mypackage;

import java.time.LocalTime;
import java.time.format.DateTimeFormatter;

public class date7 {
    public static void main(String[] args) {
        LocalTime time = LocalTime.now();
        DateTimeFormatter dtf = DateTimeFormatter.ofPattern("ss");
        System.out.println("Current Seconds: " + time.format(dtf));
    }
}
Program 8: Current Month (Abbreviated)
java
package Mypackage;

import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

public class date8 {
    public static void main(String[] args) {
        LocalDate date = LocalDate.now();
        System.out.println("Month: " + date.format(DateTimeFormatter.ofPattern("MMM")));
    }
}
Program 9: All Days of the Week (Full Names)
java
package Mypackage;

import java.time.DayOfWeek;
import java.time.format.TextStyle;
import java.util.Locale;

public class date9 {
    public static void main(String[] args) {
        System.out.println("Days of the week:");
        for (DayOfWeek day : DayOfWeek.values()) {
            System.out.println(day.getDisplayName(TextStyle.FULL, Locale.ENGLISH));
        }
    }
}
Program 10: Add Days, Hours, Minutes to Current DateTime
java
package Mypackage;

import java.time.LocalDateTime;

public class date10 {
    public static void main(String[] args) {
        LocalDateTime now = LocalDateTime.now();
        LocalDateTime modified = now.plusDays(5).plusHours(2).plusMinutes(30);
        System.out.println("Original: " + now);
        System.out.println("Modified: " + modified);
    }
}
Program 11: US vs UK Date Formats
java
package Mypackage;

import java.time.ZonedDateTime;
import java.time.format.DateTimeFormatter;
import java.util.Locale;

public class date11 {
    public static void main(String[] args) {
        ZonedDateTime now = ZonedDateTime.now();
        System.out.println("US: " + now.format(DateTimeFormatter.ofPattern("MM/dd/yyyy", Locale.US)));
        System.out.println("UK: " + now.format(DateTimeFormatter.ofPattern("dd/MM/yyyy", Locale.UK)));
    }
}
Program 12: German and French Month/Day Formats
java
package Mypackage;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.Locale;

public class date12 {
    public static void main(String[] args) {
        LocalDateTime now = LocalDateTime.now();
        System.out.println("German: " + now.format(DateTimeFormatter.ofPattern("EEEE, MMMM", Locale.GERMANY)));
        System.out.println("French: " + now.format(DateTimeFormatter.ofPattern("EEEE, MMMM", Locale.FRANCE)));
    }
}
Program 13: Rolled Hour (Current Hour + 5)
java
package Mypackage;

import java.time.LocalDateTime;

public class date13 {
    public static void main(String[] args) {
        LocalDateTime now = LocalDateTime.now();
        int rolledHour = (now.getHour() + 5) % 24;
        System.out.println("Current Hour: " + now.getHour());
        System.out.println("Rolled Hour (+5): " + rolledHour);
    }
}
Program 14: Current Week of the Year
java
package Mypackage;

import java.time.LocalDate;
import java.time.temporal.WeekFields;
import java.util.Locale;

public class date14 {
    public static void main(String[] args) {
        LocalDate today = LocalDate.now();
        int week = today.get(WeekFields.of(Locale.getDefault()).weekOfYear());
        System.out.println("Current Week of the Year: " + week);
    }
}
Program 15: Multiple Date Formats (Date only)
java
package Mypackage;

import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

public class date15 {
    public static void main(String[] args) {
        LocalDate date = LocalDate.now();
        String[] formats = {"dd-MM-yyyy", "yyyy/MM/dd", "E, MMM dd 'yy"};
        for (String pattern : formats) {
            System.out.println(pattern + " -> " + date.format(DateTimeFormatter.ofPattern(pattern)));
        }
    }
}
AVA ASSIGNMENT - COLLECTIONS
1. Sort an ArrayList of Strings alphabetically
java
import java.util.*;

class q1 {
    public static void main(String[] args) {
        ArrayList<String> neww = new ArrayList<>();
        neww.add("Ankush");
        neww.add("Madhan");
        neww.add("Gokul");
        Collections.sort(neww);
        System.out.println("Sorted Array: " + neww);
    }
}
2. Find the maximum element from an ArrayList of Integers
java
import java.util.*;

class q2 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>(Arrays.asList(1, 2, 45, 6453, 17, -34));
        System.out.println("Max element in the array is: " + Collections.max(neww));
    }
}
3. Remove all duplicate elements from an ArrayList
java
import java.util.*;

class q3 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>(Arrays.asList(1, 2, 1, 23, 2, 2, 4, 1, 3));
        Set<Integer> set = new HashSet<>(neww);
        System.out.println("List after removing duplicates: " + set);
    }
}
4. Create an ArrayList and perform add, remove, and iterate operations
java
import java.util.*;

class q4 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>();
        neww.add(97);
        neww.add(23);
        neww.add(27);
        neww.add(90);
        neww.remove(Integer.valueOf(23));
        for (Integer i : neww) {
            System.out.println(i);
        }
    }
}
5. Find the intersection of two ArrayLists
java
import java.util.*;

class q5 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>(Arrays.asList(10, 20, 30, 40, 50));
        ArrayList<Integer> old = new ArrayList<>(Arrays.asList(30, 40, 60, 70));
        neww.retainAll(old);
        System.out.println("Intersection: " + neww);
    }
}
6. Shuffle the elements of an ArrayList
java
import java.util.*;

class q6 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>(Arrays.asList(10, 20, 30, 40, 50));
        System.out.println("Before shuffle: " + neww);
        Collections.shuffle(neww);
        System.out.println("After shuffle: " + neww);
    }
}
7. Find the second-largest element in an ArrayList of Integers
java
import java.util.*;

class q7 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>(Arrays.asList(10, 20, 30, 40, 50));
        neww.remove(Collections.max(neww));
        System.out.println("Second largest element in the array is: " + Collections.max(neww));
    }
}
8. Find the frequency of each element in an ArrayList
java
import java.util.*;

class q8 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>(Arrays.asList(10, 20, 30, 40, 50, 10, 20, 90, 50));
        HashMap<Integer, Integer> dict = new HashMap<>();
        for (Integer i : neww) {
            dict.put(i, dict.getOrDefault(i, 0) + 1);
        }
        System.out.println("Element frequencies:");
        for (Map.Entry<Integer, Integer> entry : dict.entrySet()) {
            System.out.println(entry.getKey() + " -> " + entry.getValue());
        }
    }
}
9. Find the kth smallest element in an ArrayList
java
import java.util.*;

class q9 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>(Arrays.asList(345, 56, 2, 33, 56, -1, 2, 3, 550, -223));
        Scanner in = new Scanner(System.in);
        System.out.print("Enter k: ");
        int k = in.nextInt();
        int minn = 0;
        for (int i = 0; i < k; i++) {
            minn = Collections.min(neww);
            neww.remove(Integer.valueOf(minn));
        }
        System.out.println("The " + k + "th smallest number is: " + minn);
        in.close();
    }
}
10. Merge two ArrayLists into a single ArrayList
java
import java.util.*;

class q10 {
    public static void main(String[] args) {
        ArrayList<Integer> neww = new ArrayList<>(Arrays.asList(10, 20, 30));
        ArrayList<Integer> old = new ArrayList<>(Arrays.asList(40, 50, 60));
        System.out.println("List1: " + neww);
        System.out.println("List2: " + old);
        neww.addAll(old);
        System.out.println("Merged list: " + neww);
    }
}
11. Create a LinkedList and perform add, remove, and iterate operations
java
import java.util.*;

class q11 {
    public static void main(String[] args) {
        LinkedList<Integer> neww = new LinkedList<>();
        neww.addFirst(10);
        neww.addFirst(20);
        neww.addFirst(30);
        neww.addFirst(40);
        neww.addFirst(45);
        neww.addFirst(60);
        System.out.println("Original list:");
        for (Integer i : neww) {
            System.out.println(i);
        }
        neww.remove(Integer.valueOf(20));
        neww.remove(Integer.valueOf(30));
        neww.remove(Integer.valueOf(45));
        neww.remove(Integer.valueOf(60));
        System.out.println("After removing: " + neww);
    }
}
12. Implement a Queue using the LinkedList class
java
import java.util.*;

class q12 {
    public static void enqueue(LinkedList<Integer> listt, int num) {
        listt.add(num);
    }

    public static int dequeue(LinkedList<Integer> listt) {
        if (listt.isEmpty()) {
            System.out.println("Queue is empty!");
            return -1;
        }
        return listt.remove();
    }

    public static int peekfront(LinkedList<Integer> listt) {
        if (listt.isEmpty()) {
            System.out.println("Queue is empty!");
            return -1;
        }
        return listt.peek();
    }

    public static void traverse(LinkedList<Integer> listt) {
        for (Integer i : listt)
            System.out.println(i);
    }

    public static void main(String[] args) {
        LinkedList<Integer> queue = new LinkedList<>();

        enqueue(queue, 27);
        enqueue(queue, 37);
        enqueue(queue, 56);
        enqueue(queue, 256);
        enqueue(queue, 7859);
        enqueue(queue, 0);
        enqueue(queue, 1);

        System.out.println("Queue elements:");
        traverse(queue);

        System.out.println("Dequeued element: " + dequeue(queue));
        System.out.println("Dequeued element: " + dequeue(queue));
        System.out.println("Dequeued element: " + dequeue(queue));
        System.out.println("Dequeued element: " + dequeue(queue));
        System.out.println("\nFront element is: " + peekfront(queue));
    }
}
13. Implement a Stack using the LinkedList class
java
import java.util.*;

class q13 {
    public static void push(LinkedList<Integer> stack, int num) {
        stack.addLast(num);
    }

    public static int pop(LinkedList<Integer> stack) {
        if (stack.isEmpty()) {
            System.out.println("Stack is empty!");
            return -1;
        }
        return stack.removeLast();
    }

    public static int peek(LinkedList<Integer> stack) {
        if (stack.isEmpty()) {
            System.out.println("Stack is empty!");
            return -1;
        }
        return stack.getLast();
    }

    public static void traverse(LinkedList<Integer> stack) {
        System.out.println("Stack elements (bottom -> top):");
        for (Integer i : stack) {
            System.out.println(i);
        }
    }

    public static void main(String[] args) {
        LinkedList<Integer> stack = new LinkedList<>();

        push(stack, 10);
        push(stack, 20);
        push(stack, 30);
        push(stack, 40);

        traverse(stack);
        System.out.println("\nTop element is: " + peek(stack));
        System.out.println("Popped element: " + pop(stack));
        System.out.println("Popped element: " + pop(stack));

        System.out.println();
        traverse(stack);
        System.out.println("\nTop element is: " + peek(stack));
    }
}
14. Check if two LinkedLists are equal
java
import java.util.*;

class q14 {
    public static void main(String[] args) {
        LinkedList<Integer> neww = new LinkedList<>(Arrays.asList(1, 2, 3, 4, 5, 6, 7, 8));
        LinkedList<Integer> old = new LinkedList<>(Arrays.asList(1, 2, 3, 4, 5, 6, 9, 8));

        if (neww.equals(old)) {
            System.out.println("List1 and List2 are equal");
        } else {
            System.out.println("List1 and List2 are NOT equal");
        }
    }
}
1. Write a program to iterate HashMap
java
import java.util.HashMap;

public class IterateHashMap {
    public static void main(String[] args) {
        HashMap<Integer, String> fruits = new HashMap<>();
        fruits.put(1, "Apple");
        fruits.put(2, "Banana");
        fruits.put(3, "Mango");
        fruits.put(4, "Orange");

        System.out.println("Fruits List: " + fruits);
        System.out.println("Fruit with key 2: " + fruits.get(2));

        System.out.println("Iterating HashMap:");
        for (Integer key : fruits.keySet()) {
            System.out.println("Key: " + key + " Value: " + fruits.get(key));
        }
    }
}
2. Write a program to check if a key exists in a HashMap
java
import java.util.HashMap;

public class CheckKeyExample {
    public static void main(String[] args) {
        HashMap<Integer, String> fruits = new HashMap<>();
        fruits.put(1, "Apple");
        fruits.put(2, "Banana");
        fruits.put(3, "Mango");

        int keyToCheck = 2;
        if (fruits.containsKey(keyToCheck)) {
            System.out.println("Key " + keyToCheck + " exists.");
            System.out.println("Value: " + fruits.get(keyToCheck));
        } else {
            System.out.println("Key " + keyToCheck + " does not exist.");
        }
    }
}
3. Java Program to find the occurrence of words in a String using HashMap
java
import java.util.HashMap;

public class WordOccurrence {
    public static void main(String[] args) {
        String sentence = "apple banana apple mango banana apple";
        HashMap<String, Integer> wordCount = new HashMap<>();
        String[] words = sentence.split(" ");

        for (String word : words) {
            if (wordCount.containsKey(word)) {
                wordCount.put(word, wordCount.get(word) + 1);
            } else {
                wordCount.put(word, 1);
            }
        }

        System.out.println("Word Occurrence:");
        for (String word : wordCount.keySet()) {
            System.out.println(word + " : " + wordCount.get(word));
        }
    }
}
4. Write a program to copy one HashMap to another HashMap
java
import java.util.HashMap;

public class CopyHashMapExample {
    public static void main(String[] args) {
        HashMap<Integer, String> original = new HashMap<>();
        original.put(1, "Apple");
        original.put(2, "Banana");
        original.put(3, "Mango");

        HashMap<Integer, String> copy = new HashMap<>();
        copy.putAll(original);

        System.out.println("Original HashMap: " + original);
        System.out.println("Copied HashMap: " + copy);
    }
}
5. Java Program to print all duplicate characters in a String
java
import java.util.HashMap;

public class DuplicateCharacters {
    public static void main(String[] args) {
        String str = "programming";
        HashMap<Character, Integer> charCount = new HashMap<>();
        str = str.toLowerCase();

        for (int i = 0; i < str.length(); i++) {
            char ch = str.charAt(i);
            if (charCount.containsKey(ch)) {
                charCount.put(ch, charCount.get(ch) + 1);
            } else {
                charCount.put(ch, 1);
            }
        }

        System.out.println("Duplicate characters:");
        for (Character ch : charCount.keySet()) {
            if (charCount.get(ch) > 1) {
                System.out.println(ch + " : " + charCount.get(ch));
            }
        }
    }
}
6. Java Program to convert HashMap to TreeMap
java
import java.util.HashMap;
import java.util.TreeMap;

public class HashMapToTreeMap {
    public static void main(String[] args) {
        HashMap<Integer, String> hashMap = new HashMap<>();
        hashMap.put(3, "Mango");
        hashMap.put(1, "Apple");
        hashMap.put(4, "Orange");
        hashMap.put(2, "Banana");

        System.out.println("HashMap: " + hashMap);

        TreeMap<Integer, String> treeMap = new TreeMap<>(hashMap);
        System.out.println("TreeMap (Sorted by Keys): " + treeMap);
    }
}
7. Write a program to sort HashSet
java
import java.util.HashSet;
import java.util.TreeSet;

public class SortHashSet {
    public static void main(String[] args) {
        HashSet<String> fruits = new HashSet<>();
        fruits.add("Mango");
        fruits.add("Apple");
        fruits.add("Banana");
        fruits.add("Orange");

        System.out.println("Original HashSet: " + fruits);

        TreeSet<String> sortedSet = new TreeSet<>(fruits);
        System.out.println("Sorted HashSet: " + sortedSet);
    }
}
8. Write a program to convert Array to HashSet
java
import java.util.*;

public class ArrayToHashSet {
    public static void main(String[] args) {
        String[] arr = {"Apple", "Banana", "Mango", "Apple"};
        HashSet<String> set = new HashSet<>(Arrays.asList(arr));
        System.out.println("HashSet: " + set);
    }
}
9. Write a program to add elements in Stack Collection
java
import java.util.*;

public class StackExample {
    public static void main(String[] args) {
        Stack<String> stack = new Stack<>();
        stack.push("Apple");
        stack.push("Banana");
        stack.push("Mango");

        System.out.println("Stack: " + stack);
        System.out.println("Top element: " + stack.peek());
        System.out.println("Popped element: " + stack.pop());
        System.out.println("Stack after pop: " + stack);
    }
}
10. Write a program to implement a Circular Queue
java
class CircularQueue {
    int[] queue;
    int front = -1, rear = -1;
    int size;

    CircularQueue(int size) {
        this.size = size;
        queue = new int[size];
    }

    void enqueue(int data) {
        if ((rear + 1) % size == front) {
            System.out.println("Queue is Full");
            return;
        }
        if (front == -1) front = 0;
        rear = (rear + 1) % size;
        queue[rear] = data;
    }

    void display() {
        if (front == -1) {
            System.out.println("Queue is Empty");
            return;
        }
        int i = front;
        while (true) {
            System.out.print(queue[i] + " ");
            if (i == rear) break;
            i = (i + 1) % size;
        }
        System.out.println();
    }

    public static void main(String[] args) {
        CircularQueue cq = new CircularQueue(5);
        cq.enqueue(10);
        cq.enqueue(20);
        cq.enqueue(30);
        cq.display();
    }
}
11. Write a program to convert a List to a Queue
java
import java.util.*;

public class ListToQueue {
    public static void main(String[] args) {
        List<String> list = Arrays.asList("Apple", "Banana", "Mango");
        Queue<String> queue = new LinkedList<>(list);
        System.out.println("Queue: " + queue);
    }
}
12. Create a TreeMap to store the mappings of words to their frequencies in a given text
java
import java.util.*;

public class TreeMapFrequency {
    public static void main(String[] args) {
        String text = "apple banana apple mango banana";
        TreeMap<String, Integer> map = new TreeMap<>();

        for (String word : text.split(" ")) {
            map.put(word, map.getOrDefault(word, 0) + 1);
        }

        System.out.println("Word Frequencies: " + map);
    }
}
13. Implement a PriorityQueue and add elements, then print elements in sorted order
java
import java.util.*;

public class PriorityQueueExample {
    public static void main(String[] args) {
        PriorityQueue<Integer> pq = new PriorityQueue<>();
        pq.add(30);
        pq.add(10);
        pq.add(20);

        System.out.println("PriorityQueue Elements (sorted order):");
        while (!pq.isEmpty()) {
            System.out.println(pq.poll());
        }
    }
}
14. Implement a Stack using the ArrayDeque class
java
import java.util.*;

public class StackUsingDeque {
    public static void main(String[] args) {
        ArrayDeque<String> stack = new ArrayDeque<>();
        stack.push("Apple");
        stack.push("Banana");
        stack.push("Mango");

        System.out.println("Stack: " + stack);
        System.out.println("Pop: " + stack.pop());
        System.out.println("Stack after pop: " + stack);
    }
}
15. Implement a Queue using the ArrayDeque class
java
import java.util.*;

public class QueueUsingDeque {
    public static void main(String[] args) {
        ArrayDeque<String> queue = new ArrayDeque<>();
        queue.offer("Apple");
        queue.offer("Banana");
        queue.offer("Mango");

        System.out.println("Queue: " + queue);
        System.out.println("Poll: " + queue.poll());
        System.out.println("Queue after poll: " + queue);
    }
}
1. LIST FILES IN  A DIRECTORY
  
      import java.io.File;

public class ListFiles {
    public static void main(String[] args) {
        File folder = new File(".");

        File[] files = folder.listFiles();

        for (File file : files) {
            if (file.isFile()) {
                System.out.println(file.getName());
            }
        }
    }
}

2.CREATE A NEW FILE AND ADD DATA

   import java.io.FileWriter;
import java.io.IOException;

public class CreateFile {
    public static void main(String[] args) throws IOException {
        FileWriter fw = new FileWriter("sample.txt");
        fw.write("Hello this is a new file.");
        fw.close();

        System.out.println("File created and data written.");
    }
}

3.READ  DATA FROM THE USER AND ADD IT TO A NEW FILE

  import java.io.FileWriter;
import java.util.Scanner;

public class UserToFile {
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.println("Enter text:");
        String text = sc.nextLine();

        FileWriter fw = new FileWriter("newfile.txt");
        fw.write(text);
        fw.close();
    }
}

4.READ DATA FROM THE USER AND TO THE EXISTING FILE

    import java.io.FileWriter;
import java.util.Scanner;

public class AppendFile {
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.println("Enter text:");
        String text = sc.nextLine();

        FileWriter fw = new FileWriter("sample.txt", true);
        fw.write(text);
        fw.close();
    }
}

5.READ DATA FROM THE FILE AND PRINT IT TO CONSOLE

import java.io.File;
import java.util.Scanner;

public class ReadFile {
    public static void main(String[] args) throws Exception {
        File file = new File("sample.txt");
        Scanner sc = new Scanner(file);

        while(sc.hasNextLine()) {
            System.out.println(sc.nextLine());
        }
    }
}

6.TRANSFER CONTENT FROM ONE FILE TO ANOTHER

import java.io.*;

public class CopyFile {
    public static void main(String[] args) throws Exception {
        FileReader fr = new FileReader("source.txt");
        FileWriter fw = new FileWriter("dest.txt");

        int ch;
        while((ch = fr.read()) != -1) {
            fw.write(ch);
        }

        fr.close();
        fw.close();
    }
}

7.STORE FILE LINES FROM THE VARIABLE

import java.io.File;
import java.util.Scanner;

public class StoreLines {
    public static void main(String[] args) throws Exception {
        File file = new File("sample.txt");
        Scanner sc = new Scanner(file);

        String content = "";

        while(sc.hasNextLine()) {
            content += sc.nextLine() + "\n";
        }

        System.out.println(content);
    }
}

8.READ FIRST 3 LINES OF A FILE

import java.io.File;
import java.util.Scanner;

public class FirstThreeLines {
    public static void main(String[] args) throws Exception {
        File file = new File("sample.txt");
        Scanner sc = new Scanner(file);

        for(int i=1;i<=3 && sc.hasNextLine();i++) {
            System.out.println(sc.nextLine());
        }
    }
}

9.FIND LONGEST WORD IN A FILE

  import java.io.File;
import java.util.Scanner;

public class LongestWord {
    public static void main(String[] args) throws Exception {
        File file = new File("sample.txt");
        Scanner sc = new Scanner(file);

        String longest = "";

        while(sc.hasNext()) {
            String word = sc.next();
            if(word.length() > longest.length()) {
                longest = word;
            }
        }

        System.out.println("Longest word: " + longest);
    }
}
Java Assignment - JDBC Database Connection
java
import java.sql.*;

public class SimpleDBConnect {
    public static void main(String[] args) {
        try {
            // 1. Load driver (optional for newer Java versions)
            Class.forName("com.mysql.cj.jdbc.Driver");
            
            // 2. Connect to database
            Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/mydb", "root", "");
            
            // 3. Create statement
            Statement stmt = con.createStatement();
            
            // 4. Execute query
            ResultSet rs = stmt.executeQuery("SELECT * FROM employee");
            
            // 5. Display results
            while (rs.next()) {
                System.out.println(rs.getInt("EmpID") + " | " + 
                                   rs.getString("EmpName") + " | " + 
                                   rs.getString("Dept") + " | " + 
                                   rs.getInt("Salary"));
            }
            
            // 6. Close connection
            con.close();
            
        } catch (Exception e) {
            System.out.println("Error: " + e.getMessage());
        }
    }
}
1. Write a Java program to SELECT data from database using PreparedStatement where the selection criteria is taken as input from user.
import java.sql.*;
import java.util.Scanner;

public class SelectData{
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter rollno: ");
        int id = sc.nextInt();

        Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/student", "root", "password");

        String query = "SELECT * FROM students WHERE rollno = ?";
        PreparedStatement ps = con.prepareStatement(query);
        ps.setInt(1, rollno);

        ResultSet rs = ps.executeQuery();

        while (rs.next()) {
            System.out.println(rs.getInt("rollno") + " " +
                               rs.getString("name") + " " +
                               rs.getInt("age"));
        }

        con.close();
    }
}


2. Write a Java program to INSERT data into database using Statement where values are taken from user.
import java.sql.*;
import java.util.Scanner;

public class InsertData{
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter RollNo: ");
        int id = sc.nextInt();
        System.out.print("Enter name: ");
        String name = sc.next();
        System.out.print("Enter age: ");
        int age = sc.nextInt();

        Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/student", "root", "password");

        Statement stmt = con.createStatement();

        String query = "INSERT INTO students VALUES(" + rollno+ ", '" + name + "', " + age + ", NULL)";
        stmt.executeUpdate(query);

        System.out.println("Inserted successfully");
        con.close();
    }
}


3. Write a Java program to INSERT data into database using PreparedStatement where values are taken from user.
import java.sql.*;
import java.util.Scanner;

public class InsertData{
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter rollno: ");
        int id = sc.nextInt();
        System.out.print("Enter name: ");
        String name = sc.next();
        System.out.print("Enter age: ");
        int age = sc.nextInt();

        Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/student", "root", "password");

        String query = "INSERT INTO students(rollno, name, age) VALUES (?, ?, ?)";
        PreparedStatement ps = con.prepareStatement(query);

        ps.setInt(1, rollno);
        ps.setString(2, name);
        ps.setInt(3, age);

        ps.executeUpdate();

        con.close();
    }
}


4. Write a Java program to UPDATE data using Statement where update value and criteria are taken from user.
import java.sql.*;
import java.util.Scanner;

public class UpdateData{
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter rollno to update: ");
        int rollno= sc.nextInt();
        System.out.print("Enter new age: ");
        int age = sc.nextInt();

        Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/student", "root", "password");

        Statement stmt = con.createStatement();

        String query = "UPDATE students SET age = " + age + " WHERE rollno = " + rollno;
        stmt.executeUpdate(query);

        System.out.println("Updated successfully");
        con.close();
    }
}


5. Write a Java program to UPDATE data using PreparedStatement where update value and criteria are taken from user.
import java.sql.*;
import java.util.Scanner;

public class UpdateData{
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter rollno: ");
        int rollno= sc.nextInt();
        System.out.print("Enter new age: ");
        int age = sc.nextInt();

        Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/students", "root", "password");

        String query = "UPDATE students SET age = ? WHERE rollno = ?";
        PreparedStatement ps = con.prepareStatement(query);

        ps.setInt(1, age);
        ps.setInt(2, rollno);

        ps.executeUpdate();

        con.close();
    }
}


6. Write a Java program to SELECT data using Statement where selection criteria is taken from user.
import java.sql.*;
import java.util.Scanner;

public class SelectData{
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter rollno: ");
        int rollno= sc.nextInt();

        Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/student", "root", "password");

        Statement stmt = con.createStatement();

        String query = "SELECT * FROM students WHERE rollno= " + rollno;
        ResultSet rs = stmt.executeQuery(query);

        while (rs.next()) {
            System.out.println(rs.getInt("rollno") + " " + rs.getString("name"));
        }

        con.close();
    }
}


7. Write a Java program to INSERT Date into database using PreparedStatement where date is taken from user.
import java.sql.*;
import java.util.Scanner;

public class InsertDate {
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter rollno: ");
        int rollno= sc.nextInt();
        System.out.print("Enter date of birth(yyyy-mm-dd): ");
        String Input = sc.next();

        Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/student", "root", "password");

        String query = "INSERT INTO students(rollno, dob) VALUES (?, ?)";
        PreparedStatement ps = con.prepareStatement(query);

        ps.setInt(1, rollno);
        ps.setDate(2, Date.valueOf(Input));

        ps.executeUpdate();
        con.close();
    }
}


8. Write a Java program to UPDATE Date of Birth using PreparedStatement where date and criteria are taken from user.
import java.sql.*;
import java.util.Scanner;

public class UpdateDOB{
    public static void main(String[] args) throws Exception {
        Scanner sc = new Scanner(System.in);

        System.out.print("Enter rollno: ");
        int rollno= sc.nextInt();
        System.out.print("Enter new Date of birth(yyyy-mm-dd): ");
        String Input = sc.next();

        Connection con = DriverManager.getConnection(
                "jdbc:mysql://localhost:3306/student", "root", "password");

        String query = "UPDATE students SET dob = ? WHERE rollno= ?";
        PreparedStatement ps = con.prepareStatement(query);

        ps.setDate(1, Date.valueOf(Input));
        ps.setInt(2, rollno);

        ps.executeUpdate();

        con.close();
    }
}







